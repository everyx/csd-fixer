<?xml version="1.0" encoding="UTF-8"?>
<schemalist gettext-domain="csd-fixer">
  <schema id="org.gnome.shell.extensions.csd-fixer"
          path="/org/gnome/shell/extensions/csd-fixer/"
          gettext-domain="csd-fixer">
    <key name="window-rules" type="a{ss}">
      <default>{}</default>
      <summary>Custom application exclusion rules</summary>
      <description>
        Dictionary mapping a window-kind fingerprint to an exclusion rule mode.
        Key format: '&lt;wmClass&gt;:client_type=&lt;wayland|x11&gt;,window_type=&lt;int&gt;,has_parent=&lt;bool&gt;,allows_resize=&lt;bool&gt;,attached_dialog=&lt;bool&gt;'
        e.g. 'wechat:client_type=wayland,window_type=0,has_parent=true,allows_resize=false,attached_dialog=false'
        The fingerprint is derived from the picked window's stable structural
        attributes; rules never generalize to every window of an application.
        Value modes:
        - 'all': disable all decorations (no shadow, no corners)
        - 'clip': disable rounded corners (retaining shadow)
        - 'shadow': disable shadow (retaining rounded corners)
        (Legacy 'disable-*' values are transparently accepted and migrated)
      </description>
    </key>
    <key name="prefer-crisp-text" type="b">
      <default>false</default>
      <summary>Prioritize crisp text rendering</summary>
      <description>
        Skip rounded corners on fractional scale monitors (retaining shadow) to avoid text blur and resampling overhead.
      </description>
    </key>
  </schema>
</schemalist>
