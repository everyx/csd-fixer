<?xml version="1.0" encoding="UTF-8"?>
<schemalist>
  <schema id="org.gnome.shell.extensions.csd-fixer"
          path="/org/gnome/shell/extensions/csd-fixer/">
    <key name="window-rules" type="a{ss}">
      <default>{}</default>
      <summary>针对指定窗口/应用的自定义排除规则</summary>
      <description>
        以 wm_class 或 app_id 为键，值为规则模式：
        'disable-all'：完全禁用（不加阴影，不裁圆角）；
        'disable-clip'：禁用圆角裁切（保留阴影）；
        'disable-shadow'：禁用阴影（保留圆角裁切）。
      </description>
    </key>
    <key name="prefer-crisp-text" type="b">
      <default>false</default>
      <summary>优先保证文字清晰</summary>
      <description>
        在分数缩放屏幕上免除圆角（保留阴影），消除重采样带来的文字模糊与性能损耗。整数缩放不受影响。
      </description>
    </key>
  </schema>
</schemalist>
